# Versions are intentionally unpinned: the project runs on Python 3.12 (Ubuntu 24.04)
# and 3.14, and old pins lack wheels for newer Pythons (lxml/numpy then try to build
# from source and fail). pip will resolve compatible wheels for the running version.

# Web scraping and automation
playwright
requests
beautifulsoup4
lxml

# CAPTCHA solving
pytesseract
Pillow
# headless build: no desktop GUI libraries required (servers have no libGL)
opencv-python-headless

# Database
pymongo

# Cloud storage
boto3

# Data handling
numpy

# Date/time utilities
python-dateutil

# Configuration and environment
python-dotenv
pyyaml

# Logging and monitoring
loguru
tqdm

# Retry and error handling
tenacity

# HTTP client with better session management
httpx

# OpenAI API for CAPTCHA solving
openai
